# Codetop 刷题目录

## 目录用途

- 记录 CodeTop 高频题的题解，文件命名：`{题号}.{英文题名}.py`，如 `206.ReverseList.py`
- `_top100.json`：codetop.cc 前 100 高频题数据（2026-08-15 抓取，字段：rank/qid/title/slug/level/freq/solved）
- `_top100_review_plan.md`：12 主题分类复习计划（总进度 88/100，含每题核心考点）

## 复习进度（提问驱动模式）

先让用户口述思路，再讲解纠正；按主题分组推进。

- **第一组（迁移送分 5 题）✅ 完成**：剑指22（快慢指针）、283（就位区双指针）、88（逆向合并）、226（翻转二叉树）、169（摩尔投票）
- **第二组（DP 补强 3 题）✅ 完成**：198 打家劫舍 ✅（直线 + 环形 213 破环技巧）→ 152 乘积最大子数组 ✅（max/min 双状态 + 交叉递推）→ 718 最长重复子数组 ✅（二维「以 i,j 结尾」口径 + 不等清零 + 全表扫 max）
- **第三组（独立硬骨 4 题）✅ 完成**：227 基本计算器 II ✅（prev_op 哨兵 + `* /` 当场融合 + int(a/b) 向零取整）→ 207 课程表 ✅（三色 DFS 判环，与 Kahn 拓扑排序等价；灰节点=回边即环、`colors[y]` 而非 `y` 的比对陷阱）→ 补充题4 手撕快排 ✅（随机 pivot 双向交换 partition，与挖坑法对照；全相等数组 pivot 归中防退化 O(n²)）→ 470 Rand7→Rand10 ✅（扩域 + 拒绝：`(rand7()-1)*7+rand7()` 扩成均匀 1~49 → 取 ≤40 拒绝 41~49 → `(x-1)%10+1` 等票映射每输出 4 票；期望 rand7 调用 2.45 次）
- **12 题已补全 ✅**：进入主题复习已刷的 88 题，顺序 链表(17) → 二叉树(18) → DP(15) → 双指针滑窗(8) → 栈队列(6) → 二分(6) → 哈希(5) → 回溯(5) → 字符串(7) → 排序设计(7) → 图论(3) → 矩阵(3)
- **主题一·链表（17 题）✅ 完成**：206 ✅（已自练迭代+递归，跳过）→ 25 ✅（`>=` 边界：恰整除时 `>` 丢最后一组甚至全不翻；p0 三行接线顺序 `nxt→p0.next.next=cur→p0.next=pre→p0=nxt`；组间不重置 pre 是对的，脏指针总被收尾覆盖）→ 21 ✅（不变量口径：cur 指已合并段尾，每轮取小者接上、只动被取指针；尾接 `list1 or list2` 一行）→ 92 ✅（= 25 单组版：p0 走 left-1 步 + 两行缝合；dummy 免 left=1 特判）→ 23 ✅（堆存 `(val,i,node)` 序号防平局比节点；堆不变量=每链最多一节点在堆 → O(N log k)；串行归并是 O(Nk) 不是 O(mn)；分治每轮全员配对 k 减半 → O(N log k)，常数更小）→ 141 ✅（机制口径：环内间距 g 每轮 −1、整数递减必过 0 → 必相遇且不越过；fast 走 3 步 g −2 可跳过 0；先动后判防起点误报；dummy 口径=「头会被换才需要」）→ 142 ✅（相遇后 fast 回 head 双指针同速；推导 slow=a+b、fast=a+b+n(b+c)、fast=2×slow → **a=(n-1)(b+c)+c**）→ 143 ✅（三段式：找中点 → 反转后半 → 交错合并；`slow.next=None` 断开防环最易漏；A/B 条件两种模式各自自洽、别混用）→ 234 ✅（三段式判定版：找中点 → 反转后半 → `while p1` 逐对比较；混用翻车反例 A+reverse(slow.next) 在 [1,2] 误判）→ 160 ✅（走两程等路程设计 pa 走 a+c+b / pb 走 b+c+a；**不相交时相遇在 None**，`pa is pb` 一行通吃）→ 19 ✅（删倒数 N 用间距 n+1 找前驱；dummy 算术=扩展链倒数第 n+1 个必存在，n≤len 零特判）→ 82 ✅（整段删除：pre 锚点 + cur 侦察，**没重复才推进前驱**；内层 while 必须 `cur.next and` 守卫，重复段在链尾时裸 `cur.val==cur.next.val` 炸 None——「示例全过提交就挂」型 bug 第三次）→ 83 ✅（82 退化版：删除时 cur 不动 `cur.next=cur.next.next`，else 才前进；头必留 → 不用 dummy）→ 148 ✅（归并排序=找中点+21 合并旧件组装；**切半必须 B 条件**——A 条件 n=2 时 slow 停尾节点 → head2=None → 死循环，「切半用 B」升级为生死线；严格 O(1) → 自底向上迭代归并）→ 2 ✅（竖式逐位吐节点：t=a+b+carry，节点值 t%10、carry=t//10；逆序存储=天然从低位算起；`while l1 or l2 or carry` 把末尾进位折进循环；dummy 口径第二条=造新链统一首节点）→ 24 ✅（= 25 的 k=2 特例拍平三根线：先存 a、b → `a.next=b.next; b.next=a; p0.next=b; p0=a`；`while p0.next and p0.next.next` 天然兜奇数落单；头第一组就换 → dummy 口径①）

## 约定

- 复习以 `_top100_review_plan.md` 的主题分组与排名为纲，一次一题，等用户作答后再给反馈
- 用户答对：✅ 简短确认；答错或含糊：先给反例/不变量纠正，再继续
- 每题覆盖：思路 → 参考实现（Python）→ 复杂度 → 变种与面试追问
- 学习内容按需沉淀到 MyVault 知识库（`/consolidate`），算法笔记在 `MyVault/notes/algorithms/`
